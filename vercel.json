{
  "crons": [
    {
      "_comment": "RSS has no quota, so poll every 15 minutes. Vercel Hobby only runs cron daily — see DEPLOY.md for the free external-scheduler alternative.",
      "path": "/api/news/refresh",
      "schedule": "*/15 * * * *"
    }
  ]
}
